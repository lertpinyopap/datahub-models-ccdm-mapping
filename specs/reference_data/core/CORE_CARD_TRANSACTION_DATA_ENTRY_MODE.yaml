id: reference_core_card_transaction_data_entry_mode
extends: REFERENCE_CORE_SCD2_MANUAL
description: Maps reference card transaction data entry mode CSV data into a manual SCD2 card transaction data entry mode table.
control_data:
  business_key:
    fields:
      - CARD_TRANSACTION_DATA_ENTRY_MODE_CODE
source:
  seed:
    file: data/reference_data/core/REFERENCE.CORE.CARD_TRANSACTION_DATA_ENTRY_MODE.csv
    name: reference_core_card_transaction_data_entry_mode_seed
    schema: INTERMEDIATE
target:
  id: CARD_TRANSACTION_DATA_ENTRY_MODE
  schema: CORE
  table_name: CARD_TRANSACTION_DATA_ENTRY_MODE
  fields:
    - id: CARD_TRANSACTION_DATA_ENTRY_MODE_CODE
      source:
        pos: 0
        column: CARD_TRANSACTION_DATA_ENTRY_MODE_CODE
      data_type: varchar(20)
      transforms:
        - type: trim
      nullable: false
    - id: CARD_TRANSACTION_DATA_ENTRY_MODE_DESCRIPTION
      source:
        pos: 1
        column: CARD_TRANSACTION_DATA_ENTRY_MODE_DESCRIPTION
      data_type: varchar(16777216)
      transforms:
        - type: trim
      nullable: false
