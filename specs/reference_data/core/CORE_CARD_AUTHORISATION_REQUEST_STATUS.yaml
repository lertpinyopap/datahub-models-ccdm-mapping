id: reference_core_card_authorisation_request_status
extends: REFERENCE_CORE_SCD2_MANUAL
description: Maps reference card authorisation request status CSV data into a manual SCD2 card authorisation request status table.
control_data:
  business_key:
    fields:
      - CARD_AUTHORISATION_REQUEST_STATUS_CODE
source:
  seed:
    file: data/reference_data/core/REFERENCE.CORE.CARD_AUTHORISATION_REQUEST_STATUS.csv
    name: reference_core_card_authorisation_request_status_seed
    schema: INTERMEDIATE
target:
  id: CARD_AUTHORISATION_REQUEST_STATUS
  schema: CORE
  table_name: CARD_AUTHORISATION_REQUEST_STATUS
  fields:
    - id: CARD_AUTHORISATION_REQUEST_STATUS_CODE
      source:
        pos: 0
        column: CARD_AUTHORISATION_REQUEST_STATUS_CODE
      data_type: varchar(100)
      transforms:
        - type: trim
      nullable: false
    - id: CARD_AUTHORISATION_REQUEST_STATUS_DESCRIPTION
      source:
        pos: 1
        column: CARD_AUTHORISATION_REQUEST_STATUS_DESCRIPTION
      data_type: varchar(1000)
      transforms:
        - type: trim
      nullable: false
